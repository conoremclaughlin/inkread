/**
 * Dev-only self-driving demo: with the flag on, an empty library imports the
 * sample book automatically, and the reader then exercises annotations,
 * Markdown export, and TTS, logging milestones to Metro. Used to verify the
 * full pipeline on a simulator without UI automation tooling.
 */
export const AUTODEMO = false;
